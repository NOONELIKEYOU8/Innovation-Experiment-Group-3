name: flutter_hello_qjx_ready
description: "Advanced personalized Flutter Hello World dashboard with optional extensions for Innovation Experiment Week 14."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/avatars/qjx_avatar.png
